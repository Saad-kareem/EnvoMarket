import heroSection from "../assets/hero-section.webp";

const HeroSection = () => {
  return (
    <div>
      <div className="HeroSecton">
        <img src={heroSection} />
      </div>
    </div>
  );
};

export default HeroSection;
